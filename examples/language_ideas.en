// using libraries:
//      standard
//      math.

// variable definition
// let z as a 2D array of float32:
// let x as a list of list of float32,
let x as int16 equal 5.

// function defintion
// define [function_name] with [arg as type, ...], returning [return_type]
define func with x as float32, y as float32, returning int32:
    call printf with "%d\n", x * y,
    let result equal x times y times z,
    return result.

// function call
call func with x, y, and z.
let baz equal call time_func with x, y, and z.
